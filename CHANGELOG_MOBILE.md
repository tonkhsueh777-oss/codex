# V34 手机 9:16 换皮版变更记录

## 2026-09-13

- 以 `current_v34/` 为唯一代码母版，保留原有规则、牌组、AI、存档、抽牌、弃牌、地点和回合逻辑。
- 增加最后加载的 `mobile-portrait.css`，将现有 DOM 排成手机竖屏结构：顶部标题、左右信息栏、中央四地点棋盘、底部三张真实手牌和触控操作区。
- 使用 V34 `assets/cards/` 真实牌面与 `assets/card-back.jpg`，没有将参考图作为整页背景。
- 加入 safe-area、`100dvh`、无横向滚动、44px 触控目标和 390/393/430 宽度适配。
- 保留 `v34-skill-cinematic.css`、`src/v34-skill-cinematic-ui.js`、`src/v34-skill-cinematic-logic.js` 及五张 MP4 的中央播放和输入锁定逻辑。
- 输出 `dist-mobile/`，可直接由静态服务器部署。
- 手机操作区重新排序视觉层级：`换1张并结束` 使用高亮金色主按钮，`新游戏` 使用第二层级红色按钮；取消选择、保存牌局、规则说明降为低对比度辅助按钮。
- 进一步强调主动作：主按钮文案拆成“换一张手牌 / 结束回合”两行，按钮与“新游戏”同为 72px 高；辅助按钮压缩为 34px。
